<Window x:Class="IntuneComplianceMonitor.Views.DeviceDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Device Details"
        Height="600" Width="1058"
        WindowStartupLocation="CenterOwner"
        Background="White"
        ResizeMode="CanResize">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <Style x:Key="NonCompliantRowStyle" TargetType="DataGridRow">
            <Setter Property="Background" Value="#FFEBEB"/>
            <Setter Property="Foreground" Value="#B00020"/>
        </Style>

        <Style x:Key="CompliantRowStyle" TargetType="DataGridRow">
            <Setter Property="Background" Value="#D0FFEF"/>
            <Setter Property="Foreground" Value="Black"/>
        </Style>

        <Style x:Key="ErrorRowStyle" TargetType="DataGridRow">
            <Setter Property="Background" Value="#FFEBEB"/>
            <Setter Property="Foreground" Value="#B00020"/>
        </Style>

        <Style x:Key="NotApplicableRowStyle" TargetType="DataGridRow">
            <Setter Property="Background" Value="#F5F5F5"/>
            <Setter Property="Foreground" Value="Black"/>
        </Style>
      
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Device Header -->
        <StackPanel Orientation="Vertical" Margin="0,0,0,15">
            <TextBlock Text="{Binding Device.DeviceName}" FontSize="22" FontWeight="Bold" Foreground="Black"/>
            <TextBlock Text="{Binding Device.OSVersion}" FontSize="14" Foreground="Gray"/>
            <TextBlock Text="{Binding Device.Owner}" FontSize="14" Foreground="Gray"/>
        </StackPanel>

        <!-- Tab Content -->
        <TabControl Grid.Row="1">

            <!-- Compliance Tab -->
            <TabItem Header="Compliance">
                <DataGrid ItemsSource="{Binding PolicyStates}" 
                          x:Name="ComplianceDataGrid"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          AlternatingRowBackground="#F9F9F9"
                          RowHeaderWidth="0"
                          Margin="10">

                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F0F0F0"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Padding" Value="8,6"/>
                        </Style>
                        <Style TargetType="DataGridRow">
                            <Style.Triggers>
                                <!-- Row is selected -->
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}" />
                                    <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}" />
                                </Trigger>

                                <!-- Not Compliant -->
                                <DataTrigger Binding="{Binding State}" Value="Non-Compliant">
                                    <Setter Property="Background" Value="#FFEBEB"/>
                                    <Setter Property="Foreground" Value="#B00020"/>
                                </DataTrigger>

                                <!-- Compliant -->
                                <DataTrigger Binding="{Binding State}" Value="Compliant">
                                    <Setter Property="Background" Value="#D0FFEF"/>
                                    <Setter Property="Foreground" Value="Black"/>
                                </DataTrigger>

                                <!-- Not Applicable -->
                                <DataTrigger Binding="{Binding State}" Value="Not Applicable">
                                    <Setter Property="Background" Value="#F5F5F5"/>
                                    <Setter Property="Foreground" Value="Black"/>
                                </DataTrigger>

                                <!-- Error -->
                                <DataTrigger Binding="{Binding State}" Value="Error">
                                    <Setter Property="Background" Value="#FFEBEB"/>
                                    <Setter Property="Foreground" Value="#B00020"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>

                    </DataGrid.Resources>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Policy Name" Binding="{Binding DisplayName}" Width="*"/>
                        <DataGridTextColumn Header="State" Binding="{Binding State}" Width="150"/>
                        <DataGridTextColumn Header="User" Binding="{Binding UserPrincipalName}" Width="250"/>
                        <DataGridTextColumn Header="Last Contacted" Binding="{Binding LastReportedDateTime}" Width="200" />
                        <DataGridTextColumn Header="Error Details" Binding="{Binding ErrorDetailsList}" Width="*" 
                        />
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <!-- Configuration Profiles Tab -->
            <TabItem Header="Configuration Profiles">
                <Grid>
                    <!-- Loading Indicator -->
                    <TextBlock Text="Loading configuration profiles..."
                               FontStyle="Italic"
                               Foreground="Gray"
                               Visibility="{Binding IsLoadingProfiles, Converter={StaticResource BoolToVisibilityConverter}}"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Top"
                               Margin="0,10"/>

                    <!-- The ListBox will be shown when not loading -->
                    <ListBox ItemsSource="{Binding ConfigurationProfiles}"
                             Margin="0,30,0,0"
                             Visibility="{Binding IsLoadingProfiles, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=true}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#CCC" BorderThickness="1" Padding="10" Margin="0,0,0,10" CornerRadius="4">
                                    <StackPanel>
                                        <TextBlock Text="{Binding DisplayName}" FontWeight="Bold"/>
                                        <TextBlock Text="{Binding Description}" FontSize="12" TextWrapping="Wrap" Foreground="Gray"/>
                                        <TextBlock Text="{Binding Status}" FontSize="11" FontStyle="Italic" Foreground="SlateGray"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </TabItem>

            <!-- Device Properties -->
            <TabItem Header="Device Properties">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="10">
                    <Grid Margin="0,0,0,10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Serial Number:" FontWeight="Bold" Margin="0,5"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Device.SerialNumber}" Margin="10,5"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Manufacturer:" FontWeight="Bold" Margin="0,5"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Device.Manufacturer}" Margin="10,5"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Model:" FontWeight="Bold" Margin="0,5"/>
                        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Device.Model}" Margin="10,5"/>

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="Ownership:" FontWeight="Bold" Margin="0,5"/>
                        <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding Device.Ownership}" Margin="10,5"/>
                    </Grid>
                </ScrollViewer>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
